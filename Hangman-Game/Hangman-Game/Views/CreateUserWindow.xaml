<Window x:Class="Hangman_Game.Views.CreateUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Create Profile"
        Height="720"
        Width="850"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#130B0B">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="#F7D28D"/>
            <Setter Property="Background" Value="#2A1B1B"/>
            <Setter Property="BorderBrush" Value="#D18A2C"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3A2525"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#E3A74B"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1E1212"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#C47B1C"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#241919"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#8A6A3D"/>
                                <Setter Property="Foreground" Value="#C8A977"/>
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#F2D8A7"/>
        </Style>

        <Style x:Key="AvatarListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Create Profile"
                   FontSize="32"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Foreground="#F0C27B"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,18">
            <TextBlock Text="Select Avatar"
                       FontSize="22"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>

            <Button Content="Browse External Avatar"
                    Width="240"
                    HorizontalAlignment="Center"
                    Command="{Binding BrowseAvatarCommand}"/>
        </StackPanel>

        <Border Grid.Row="2"
                Background="#1A1010"
                BorderBrush="#5B3636"
                BorderThickness="2"
                CornerRadius="14"
                Padding="16">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ListBox ItemsSource="{Binding AvailableAvatars}"
                         SelectedItem="{Binding SelectedAvatar}"
                         BorderThickness="0"
                         Background="Transparent"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         FocusVisualStyle="{x:Null}"
                         ItemContainerStyle="{StaticResource AvatarListBoxItemStyle}" d:ItemsSource="{d:SampleData ItemCount=5}">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Width="110" Height="110" Margin="8">
                                <Rectangle x:Name="avatarBorder"
                                           RadiusX="12"
                                           RadiusY="12"
                                           Stroke="#5B3636"
                                           StrokeThickness="2">
                                    <Rectangle.Fill>
                                        <ImageBrush ImageSource="{Binding FullPath}"
                                                    Stretch="UniformToFill"/>
                                    </Rectangle.Fill>
                                </Rectangle>
                            </Grid>

                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsMouseOver}"
                                             Value="True">
                                    <Setter TargetName="avatarBorder" Property="Stroke" Value="#B6762D"/>
                                </DataTrigger>

                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}"
                                             Value="True">
                                    <Setter TargetName="avatarBorder" Property="Stroke" Value="#D18A2C"/>
                                </DataTrigger>

                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="True"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsMouseOver}" Value="True"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter TargetName="avatarBorder" Property="Stroke" Value="#E3A74B"/>
                                </MultiDataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="3" Margin="0,18,0,0">
            <TextBlock Text="Profile Name:"
                       FontSize="22"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,8"/>

            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                     Width="320"
                     Height="44"
                     HorizontalAlignment="Center"
                     FontSize="22"
                     Background="#1A1010"
                     Foreground="#F2D8A7"
                     BorderBrush="#D18A2C"
                     BorderThickness="2"
                     HorizontalContentAlignment="Center"
                     VerticalContentAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="4"
                    Orientation="Vertical"
                    HorizontalAlignment="Center"
                    Margin="0,10,0,0">
            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="#FF7D7D"
                       FontSize="15"
                       Margin="0,0,0,10"
                       TextAlignment="Center"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Back"
                        Width="160"
                        Command="{Binding CancelCommand}"/>
                <Button Content="Create Profile"
                        Width="200"
                        Command="{Binding CreateProfileCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>